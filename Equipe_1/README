Integrantes da Equipe:

    .Alice Martins Valero
    .Beatriz Santos de Oliveira
    .Eduardo Takeshi Watanabe
    .Júlia de Araújo Ramos
    .Murilo Patricio Maia

Os arquivos estão organizados da seguinte forma:

    Arvores:
        
        Conteúdo:
                Demontração das funções de inclusão, remoção e busca em árvores binárias usando números aleatórios.
        
        Compilação:
                $> gcc -o funcoes.o -c funcoes.c
                $> gcc -o main.o -c main.c
                $> gcc -o exemplo funcoes.o main.o

        Rodar:
                $> ./exemplo

    Arvores_Ordenadas:
        
        Conteúdo: 
                Demonstração das funções de inclusão,remoção e busca em arvores binárias e AVL usando o dataset da equipe.

        Compilação:
                $> gcc -o funcoes.o -c funcoes.c
                $> gcc -o main.o -c main.c
                $> gcc -o exemplo funcoes.o main.o

        Rodar:
                $> ./exemplo
    
    Listas:

        Conteúdo:
                No main temos uma demonstração das funções de listas ordenadas e desordenadas
                No main2 temos uma demonstração da funções gerais de inclusão,remoção e busca.

        Compilação:

            main:
                $> gcc -o funcoes.o -c funcoes.c
                $> gcc -o main.o -c main.c
                $> gcc -o exemplo funcoes.o main.o

            main2: 
                $> gcc -o funcoes.o -c funcoes.c
                $> gcc -o main2.o -c main2.c
                $> gcc -o exemplo2 funcoes.o main2.o

        Rodar:

            main:
                $>./exemplo
            
            main2:
                $>./exemplo2

    Pilhas_Filas:

        Conteúdo:
                No main temos uma demonstração das funções de pilhas e filas em lista de alocação estática
                No main2 temos uma demonstração das funções de pilhas, filas e listas de alocação dinámica.
            Esse main também demonstra a passagem de elementos entre filas, pilhas e listas e como isso
            afeta a ordem dos elementos.
        
        Compilação:
            
            main:
                $> gcc -o funcoes.o -c funcoes.c
                $> gcc -o main.o -c main.c
                $> gcc -o exemplo funcoes.o main.o

            main2:
                $> gcc -o funcoes.o -c funcoes.c
                $> gcc -o main2.o -c main2.c
                $> gcc -o exemplo2 funcoes.o main2.o

        Rodar:
            main:
                $>./exemplo

            main2:
                $>./exemplo2


            

    